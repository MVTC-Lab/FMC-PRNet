
# Parameters
nc: 10
scales:
  n: [0.50, 0.25, 1024]

backbone:
  # [from, repeats, module, args]
  - [-1, 1, ESSamp, [64]]  # 0-P1/2
  - [-1, 1, ESSamp, [128]] # 1-P2/4


  - [-1, 1, HSG_WAVE, [256]] # 2 - P2_in
  - [-1, 1, Conv, [256, 3, 2]] # 3-P3/8


  - [-1, 1, HSG_WAVE, [512]] # 4 - P3_in

  - [-1, 1, Conv, [512, 3, 2]] # 5-P4/16
  - [-1, 2, C3k2, [512, True]] # 6 - P4_in
  - [-1, 1, Conv, [1024, 3, 2]] # 7-P5/32
  - [-1, 2, C3k2, [1024, True]] # 8


  - [-1, 1, ResASPP_Optimized, [1024]] # 9
  - [-1, 2, C2PSA, [1024]] # 10


head:
  - [10, 1, nn.Upsample, [None, 2, "nearest"]] # 11
  - [[-1, 6], 1, Concat, [1]] # 12
  - [-1, 2, C3k2, [512, False]] # 13 -> P4_td

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 14
  - [[-1, 4], 1, Concat, [1]] # 15
  - [-1, 2, C3k2, [256, False]] # 16 -> P3_td

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 17
  - [[-1, 2], 1, Concat, [1]] # 18
  - [-1, 2, C3k2, [128, False]] # 19 -> P2_td


  - [-1, 1, Conv, [256, 3, 2]] # 20
  - [[-1, 4], 1, Concat, [1]] # 21
  - [-1, 2, C3k2, [256, False]] # 22 -> P3_td1


  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 23
  - [[-1, 19, 2], 1, Concat, [1]] # 24
  - [-1, 2, C3k2, [128, False]] # 25 -> P2_refine1


  - [-1, 1, Conv, [256, 3, 2]] # 26
  - [-1, 1, Conv, [512, 3, 2]] # 27
  - [-1, 2, C3k2, [512, False]] # 28

  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 29
  - [[-1, 4], 1, Concat, [1]] # 30
  - [-1, 2, C3k2, [256, False]] # 31
  - [-1, 1, nn.Upsample, [None, 2, "nearest"]] # 32
  - [[-1, 25, 2], 1, Concat, [1]] # 33
  - [-1, 2, C3k2, [128, False]] # 34

  - [[34, 31, 28], 1, Detect, [nc]] # 35